# Project Configuration: Commit Content Tracker
# This project tracks its own development and creates X threads about new features

id: commit-content-tracker
name: Commit Content Tracker
repository: amitayks/commit-content-creatore
enabled: true

# Trigger settings
triggers:
  branches:
    - main
    - release/*
  events:
    - pr_merged
    - push

# Content generation preferences
content:
  types:
    - feature_announcement
    - technical_deep_dive
    - learning_share
    - dev_update
  tone: enthusiastic

# Formatting options
formatting:
  emojis: true
  hashtags:
    always:
      - "#DevTools"
      - "#Automation"
      - "#AI"
    project:
      - "#CommitContentTracker"
      - "#GitHub"
      - "#TypeScript"

# Thread settings
thread:
  minCommitsForThread: 2
  maxTweets: 10
  alwaysGenerateImage: true
  singleTweetImageProbability: 0.7

# Debounce settings
debounce:
  pushDebounceMinutes: 30
  prImmediate: true

# Platforms
platforms:
  x: true
