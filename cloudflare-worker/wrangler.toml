name = "telegram-webhook-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Set these via `wrangler secret put <NAME>`
# - TELEGRAM_BOT_TOKEN
# - GITHUB_TOKEN  
# - GITHUB_REPO (e.g., "amitayks/commit-content-creatore")

[observability]
enabled = true
